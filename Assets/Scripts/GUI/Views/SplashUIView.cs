using GUI.ViewModels;

namespace GUI.Views
{
    public class SplashUIView : View<SplashScreenViewModel>
    {
        public override void Bind(SplashScreenViewModel viewModel)
        {
            base.Bind(viewModel);
        }
    }
}
